# CHANGELOG

## 2026-08 · Редизайн: переход от «жидкого стекла» к чистым карточкам (glass-redesign → main)

- Две темы (тёмная/светлая): переключатель в шапке, выбор в localStorage, применение до загрузки React.
- Палитра: нейтральные чёрный/белый + акцент GitHub Blue; убраны backdrop-filter, aurora, noise.
- Чистка имён: ink/paper вместо white/black; card/field/btn-primary/cell-* вместо glass*.
- Секреты: •••••••• с 👁/⧉; флаг is_secret для кастомных полей (новые колонки в БД).
- Фиксы: читаемость нативных select в тёмной теме, центровка ⧉, глазок пароля на логине.
- Решения: поэтапный перенос через алиасы без поломки релиза; акцент не привязан к favicon; секреты в xlsx оставлены открытыми (бэкап админа).
- Открытые вопросы: новый favicon под стиль; опциональная маскировка секретов в экспорте; удаление неиспользуемых ассетов (public/icons.svg, src/assets/*).
- README обновлён; ветка слита в main и удалена, деплой на gh-pages выполнен.

## 2026-08-14 — Мини-дашборд разработчика

- Добавлен мини-дашборд на страницу разработчика (роль `dev`, над журналом логов): бар-чарт входов по дням (14 дней), топ-5 заводов по просмотрам/копированиям, счётчик копирований по пользователям.
- Новые файлы: `src/features/dev/stats.ts` (чистая агрегация), `src/features/dev/DevDashboard.tsx`; изменён `src/App.tsx`. Закоммичено и задеплоено на gh-pages.
- Решения: без новых зависимостей — графики на CSS/Tailwind; данные — последние 2000 строк `audit_log` на клиенте; «входы» = только `login_success`; завод = часть `entity` до первого « · », иначе «Прочее».
- Решения (UI): компактная ось дат — только число дня, полное «дд.мм» в начале периода и при смене месяца; CSS-тултипы над столбиками (group-hover) вместо нативного `title`.
- Решения (репо): в `.gitignore` добавлены `repomix-output.txt` и `repomix.config.json`, удалён старый `project_dump.txt`; контекст для ИИ обновляется командой `npx repomix`.
- Открытые вопросы: дашборд и LogsPage тянут `audit_log` отдельными запросами (2000 и 500) — можно объединить; копирования без контекста попадают в «Прочее»; `login_failed` не показан на графике; README не упоминает мини-дашборд.