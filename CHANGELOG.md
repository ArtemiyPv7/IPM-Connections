# CHANGELOG

2026-08-17 · Технический рефакторинг структуры (без изменения функционала)
Сделано: удалён мёртвый код (useRecents/pushRecent, CardSkeletonGrid, monthKey, fetchTodayDutyName); `src/lib` слит в `src/shared/lib`; сессия 6 ч вынесена в `shared/lib/session.ts`; роль запрашивается один раз в App и раздаётся через RoleContext; SVG-знаки протоколов — в `protocol-icons.tsx` (общая обёртка Mark); общие части календаря — `dutyCell.ts` + `CellParts.tsx`; в styles.ts добавлен `iconBtnCls`; KeyValueEditor переведён на `KeyValue`/`dangerCls`; фикс `react-hooks/exhaustive-deps` в CompanyPassport (useCallback).
Ключевые решения: комментарии — только для неочевидных бизнес-правил; механические правки импортов точечно, сложные файлы — целиком; утилиты в `.ts`, компоненты в `.tsx` (под линтер `only-export-components`); MonthGrid и MonthAgenda не объединяли — адаптивные каркасы (`max-[640px]`/`min-[641px]`) сохранены, мобильность не задета.
Открытые вопросы: в файлах, которые не переписывались, комментарии остались (опциональная чистка); Vite-предупреждение о чанке >500 kB (тяжёлый `xlsx`) — при желании code-splitting экспорта; CompanyPassport осознанно оставлен без декомпозиции.

## 2026-08 · Редизайн по макету: master-detail, паспорт завода, цепочки, новый календарь дежурств

### Экран «Заводы»
- Master-detail: слева фильтр (с крестиком очистки) и список (избранные ★ сверху, далее по алфавиту), справа паспорт; на ≤900px паспорт поверх со кнопкой «‹ Заводы», фон непрозрачный.
- Строка завода: SVG-плитка протокола, название, версии «1.6.10 / 1.5.11» моно без подписей, только названия торговых групп; «Недавние» убраны.
- «/» и «/company/:id» — один экран; кнопка «+» в тон выбранного завода; плашка «Дежурит: …» переехала в шапку (смена 8:00–8:00).

### Паспорт завода
- Шапка с ★ и бейджем «не работает»; мета-чипы: Сервер/КПЛ (моно), контуры, группы «порт — группа», статус версии.
- Секции капсом: «Способы подключения» (карточки в 2 колонки: плитка, бейдж типа, копи-кнопки у значений), «Дополнительно», «Файлы и ссылки» (конфиги/веб всех подключений), «История и заметки».
- Формы завода и подключений — в модальных окнах (включая «/company/new»); модалки через портал — fixed не ломается из-за transform-предков.

### Иконки протоколов
- SVG-знаки вместо эмодзи: AnyDesk (шевроны), VPN (щит со скважиной), OpenVPN (замок), Kontur (ноутбук с «>»), WireGuard (молния), RuDesktop (монитор со стрелкой), VNC (монитор с курсором), RustDesk (шестерёнка), Ammyy (кейс), custom (слайдеры), запасной (штекер); RDP — 🖥️.

### Цепочки подключений
- Миграция: `connections.chain_id text`, `connections.chain_step integer not null default 0`.
- В форме поля «Цепочка» (с подсказками существующих) и «Номер шага»; карточка-цепочка с нумерацией и соединительной линией, в одной сетке с одиночными.

### Формы подключений
- Подписи над полями, секции капсом, человеческие имена типов, динамические плейсхолдеры адреса, название необязательно (фолбэк — имя типа).
- Пароль: `type="text"` + CSS-маска `secret-mask` — браузер не предлагает сохранить; на логине `autoComplete="new-password"`.

### Экран «Дежурства»
- Сводка-чипы: дежурный (правило 8:00), ДР «через N дней», отпуска (активные/ближайшие).
- Master-detail: слева месяцы с годом ‹ ›; справа выбранный месяц, на ≥1536px рядом следующий (экспорт — один, для левого); на ≤640px — агенда списком.
- «Сегодня» — выделение как у выбранного завода (без градиента); пунктир выходных убран; отпуска — приглушённая тонировка (0.08 в тёмной, 0.07 в светлой).
- Редактор смены в модалке; «Сотрудники» и «Отпуска» — раскрывающиеся карточки (свёрнуты по умолчанию): компактные строки, ✎/✕ у имён, отпуска «29.06 — 05.07», добавление в модалках.

### Общее
- Интерфейс в скруглённом «окне» (rounded-3xl), шапка на всю ширину, лого-плитка IPM; роль одной буквой (A/S/D), на мобильном бейдж = выход.
- Копи-кнопки — SVG (два квадрата), всегда по центру.

### База
- Таблица `vacations` + RLS (чтение — authenticated, запись — admin).

### Открытые вопросы
- KeyValueEditor и заметки истории остались inline (модалки только для завод/подключение/смена/сотрудник/отпуск).
- `-webkit-text-security` не работает в Firefox — пароль в формах там виден открытым (приемлемо для внутреннего сервиса).

## 2026-08 · Редизайн: переход от «жидкого стекла» к чистым карточкам (glass-redesign → main)

- Две темы (тёмная/светлая): переключатель в шапке, выбор в localStorage, применение до загрузки React.
- Палитра: нейтральные графит/белый + акцент GitHub Blue; убраны backdrop-filter, aurora, noise.
- Чистка имён: ink/paper вместо white/black; card/field/btn-primary/cell-* вместо glass*.
- Секреты: •••••••• с 👁/⧉; флаг is_secret для карточных полей (новые колонки в БД).
- Фиксы: читаемость нативных select в тёмной теме, центровка ⧉, глазок пароля на логине.
- Решения: поэтапный перенос через алиасы без поломки релиза; акцент не привязан к favicon; тексты в xlsx оставлены открытыми (бэкап админа).
- README обновлён; ветка слита в main и удалена, деплой на gh-pages выполнен.

## 2026-08-14 · Мини-дашборд разработчика

- Добавлен мини-дашборд на странице разработчика (роль `dev`, над журналом логов): бар-чарт входов по дням (14 дней), топ-5 заводов по просмотрам/копированиям, счётчик копирований по пользователям.
- Новые файлы: `src/features/dev/stats.ts` (чистая агрегация), `src/features/dev/DevDashboard.tsx`; изменён `src/App.tsx`. Закоммичено и задеплоено на gh-pages.
- Решения: без новых зависимостей — графики на CSS/Tailwind; данные — последние 2000 строк `audit_log` на клиенте; «вход» = только `login_success`; завод = часть `entity` до первого «·», иначе «Прочее».
- Решения (UI): компактная ось дат — только число дня, полное «дд.мм» в начале периода и при смене месяца; CSS-тултипы над столбиками (group-hover) вместо нативного `title`.
- Решения (репо): в `.gitignore` добавлены `repomix-output.txt` и `repomix.config.json`, удалён старый `project_dump.txt`; контекст для ИИ обновляется командой `npx repomix`.
- Открытые вопросы: дашборд и LogsPage ходят в `audit_log` отдельными запросами (2000 и 500) — можно объединить; копирования без контекста попадают в «Прочее»; `login_failed` не показан на графике; README не упоминал мини-дашборд.